// Vitest setup: mock date-fns/locale for node environment tests
// This prevents ESM/CJS resolution issues when running tests

import { beforeAll } from 'vitest';

beforeAll(() => {
  // Setup completed
});
